# Dockerfile Hardening Progress

Started: 2026-04-14 (post-static-audit)
Status: COMPLETE

## Blockers
- [x] B1. zsteg moved from apt → `gem install one_gadget zsteg`.
- [x] B2. Ghidra 12.0.4 layer added with default-jdk-headless + analyzeHeadless symlink.
- [x] B3. Removed `nodejs`/`npm` from `docker/apt-packages.txt`.

## High
- [x] H1a. Added patchelf, p7zip-full, jadx, sleuthkit, binaryen to apt-packages.txt.
- [x] H1b. Added playwright, uncompyle6, lief, pyinstxtractor-ng, pillow to requirements-ctf.txt.
- [x] H2. Added `playwright install --with-deps chromium` layer after pip.
- [x] H3. Verified: ubuntu:24.04 has universe enabled by default; no change needed.

## Medium / polish
- [x] M2. Updated rev-specialist (pyinstxtractor-ng; cfr-fallback note; uncompyle6 3.8-cap).
- [x] M3. Added targeted Dockerfile comments on zsteg/gems, Ghidra ARG pin, playwright deps timing, sleuthkit+binaryen.

## Verification
- [x] V1. pytest 73/73 green.
- [x] V2. ruff clean.
- [x] V3. Dockerfile: no apt zsteg; single nodejs (NodeSource); Ghidra layer + /usr/local/bin/analyzeHeadless symlink present.
- [x] V4. All 6 specialists cross-checked against installed tools; mismatches resolved (libc-database kept as ad-hoc clone, stegsolve as optional note).
- [x] V5. docs/audit-2026-04-14.md appended with `## Dockerfile hardening pass (2026-04-14)` section.
- [x] V6. Pushed 0f48fdb..4dd6e1c.
