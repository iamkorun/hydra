# Dockerfile Hardening Progress

Started: 2026-04-14 (post-static-audit)

## Blockers
- [ ] B1. Remove `zsteg` from forensics apt layer; move to `gem install` layer.
- [ ] B2. Install Ghidra 11.x + default-jdk-headless + symlink analyzeHeadless.
- [ ] B3. Remove `nodejs`, `npm` from `docker/apt-packages.txt` (NodeSource layer installs).

## High
- [ ] H1a. Add to apt-packages.txt: patchelf, p7zip-full, jadx, cfr (verify availability on noble).
- [ ] H1b. Add to requirements-ctf.txt: playwright, uncompyle6, lief (+ optional pyinstaller/pyinstxtractor).
- [ ] H2. Add `playwright install --with-deps chromium` layer after pip.
- [ ] H3. Verify sagemath/universe — add `software-properties-common`/`add-apt-repository universe` if risky.

## Medium / polish
- [ ] M2. Update rev-specialist if cfr/jadx end up unavailable.
- [ ] M3. Add ≤4 targeted Dockerfile comments.

## Verification
- [ ] V1. `.venv/bin/pytest` — 73+ green.
- [ ] V2. `.venv/bin/ruff check .` — clean.
- [ ] V3. Re-read Dockerfile; no `zsteg` in apt; no dup nodejs; ghidra layer + symlink present.
- [ ] V4. Re-read all 6 specialist .md — every tool they mention is installed or marked optional.
- [ ] V5. Update `docs/audit-2026-04-14.md` with `## Dockerfile hardening pass (2026-04-14)` section.
- [ ] V6. `git push`.

## Iteration log

- Iteration 1: created progress file. Starting with B1+B3 (minimal, no external lookups needed).
