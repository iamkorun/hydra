# Hydra Audit Progress

Started: 2026-04-14
Status: COMPLETE (7/7 sections done, 73 tests passing, pushed)

## Checklist

### 1. AUTH: switch from ANTHROPIC_API_KEY to `claude -p` subscription auth
- [x] 1a. Extend `docker_worker.run_worker` signature: `credentials_dir: Path | None = None`; mount `/root/.claude:ro` if present.
- [x] 1b. Extend `OrchestratorConfig` with `credentials_dir` + `api_key` now optional.
- [x] 1c. Update `Orchestrator._one` to pass `credentials_dir` through.
- [x] 1d. Update `cli.resolve_config`: require `~/.claude/` exists (with a credentials file) OR `ANTHROPIC_API_KEY` env var; pass whichever is set.
- [x] 1e. Dockerfile: bind-mount auto-creates `/root/.claude`, no change needed.
- [x] 1f. Unit tests: `test_docker_worker.py` covers cred mount; `test_cli.py` updates for new auth logic.
- [x] 1g. README.md now documents subscription auth (preferred) and API key (fallback).

### 2. PAPER SKILLS
- [x] 2a. Added `tmux`, `socat`, `netcat-openbsd` to `docker/apt-packages.txt`.
- [x] 2b. Created `.claude/skills/pwn/tmux-session.md`.
- [x] 2c. Created `.claude/skills/meta/subtask-decomposition.md` (Cybench).
- [x] 2d. Created `.claude/skills/meta/iat-pattern.md` (EnIGMA).
- [x] 2e. All 6 specialist prompts now lead with "Top principle: shell-first" (Palisade).
- [x] 2f. Created `notes/lessons-learned.md` scaffold.
- [x] 2g. `CLAUDE.md` references new meta skills + lessons-learned append rule.

### 3. FIX P1 DRIFT
- [x] 3a. Restore `error` vs `failed` status in `hydra/orchestrator.py`.
- [x] 3b. Update `test_mixed_batch` in `tests/integration/test_pipeline.py` (exit_code=1 → status "error").
- [x] 3c. Keep flag extractor's negative-lookbehind fix (verified present).

### 4. SCAN & FIX ISSUES
- [x] 4a. ruff installed; autofixed 10 + added per-file ignores for exploits/; clean.
- [x] 4b. Specialist paths validated — fixed 2 broken refs in rev-specialist.md.
- [x] 4c. All 15 exploit templates ast.parse cleanly.
- [x] 4d. pytest 73/73 green.

### 5. NEW TESTS
- [x] 5a. Test for `credentials_dir` mount in docker worker.
- [x] 5b. Test for `error` vs `failed` distinction in orchestrator.

### 6. AUDIT REPORT
- [x] 6. `docs/audit-2026-04-14.md` written. 3 critical + 6 high fixed, 11 medium listed, 8 out-of-scope.

### 7. PUSH
- [x] 7. Pushed: 00bdff7 → d1ce9de. Audit delivered.

## Iteration log

- Iteration 1: Created this progress file. Starting item 1a-1c (docker_worker + orchestrator + config changes).
