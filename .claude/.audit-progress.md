# Hydra Audit Progress

Started: 2026-04-14
Status: in_progress

## Checklist

### 1. AUTH: switch from ANTHROPIC_API_KEY to `claude -p` subscription auth
- [ ] 1a. Extend `docker_worker.run_worker` signature: `credentials_dir: Path | None = None`; mount `/root/.claude:ro` if present.
- [ ] 1b. Extend `OrchestratorConfig` with `credentials_dir` + `api_key` now optional.
- [ ] 1c. Update `Orchestrator._one` to pass `credentials_dir` through.
- [ ] 1d. Update `cli.resolve_config`: require `~/.claude/` exists (with a credentials file) OR `ANTHROPIC_API_KEY` env var; pass whichever is set.
- [ ] 1e. Update Dockerfile — ensure `/root/.claude` is the expected mount point (no actual chmod needed since it's bind-mount :ro).
- [ ] 1f. Unit tests: `test_docker_worker.py` covers cred mount; `test_cli.py` updates for new auth logic.
- [ ] 1g. Update README.md usage — document `claude -p` subscription auth as preferred, API key as fallback.

### 2. PAPER SKILLS
- [ ] 2a. Add `tmux` to `docker/apt-packages.txt` (for EnIGMA/NYU persistent sessions).
- [ ] 2b. Create `.claude/skills/pwn/tmux-session.md`.
- [ ] 2c. Create `.claude/skills/meta/subtask-decomposition.md` (Cybench).
- [ ] 2d. Create `.claude/skills/meta/iat-pattern.md` (EnIGMA).
- [ ] 2e. Audit specialist prompts — add "Try shell first" principle at top (Palisade plain-agent).
- [ ] 2f. Create `notes/lessons-learned.md` scaffold + instruct CLAUDE.md to append.
- [ ] 2g. Update `CLAUDE.md` to reference new skills + lessons-learned.

### 3. FIX P1 DRIFT
- [ ] 3a. Restore `error` vs `failed` status in `hydra/orchestrator.py`.
- [ ] 3b. Update `test_mixed_batch` in `tests/integration/test_pipeline.py` (exit_code=1 → status "error").
- [ ] 3c. Keep flag extractor's negative-lookbehind fix (verify).

### 4. SCAN & FIX ISSUES
- [ ] 4a. Run `ruff check .` (install if missing) and fix.
- [ ] 4b. Verify all specialist prompts reference valid paths.
- [ ] 4c. Verify exploit templates all `ast.parse` cleanly.
- [ ] 4d. Final `pytest` run green.

### 5. NEW TESTS
- [ ] 5a. Test for `credentials_dir` mount in docker worker.
- [ ] 5b. Test for `error` vs `failed` distinction in orchestrator.

### 6. AUDIT REPORT
- [ ] 6. Write `docs/audit-2026-04-14.md` with Critical/High/Medium/Out-of-scope sections.

### 7. PUSH
- [ ] 7. `git push`.

## Iteration log

- Iteration 1: Created this progress file. Starting item 1a-1c (docker_worker + orchestrator + config changes).
